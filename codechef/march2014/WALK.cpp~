#include <iostream>
#include <iomanip>
#include <cstdio>
#include <cmath>
#include <cstdlib>
#include <cctype>
#include <string>
#include <cstring>
#include <ctime>
#include <string.h>

using namespace std;


int main ()
{
    int T,n,i,j,ans,max,w,idx;	
	cin>>T;
	while(T--)
	{
		cin>>n;
		max=-1;
		for(i=0;i<n;i++) 
		{
			cin>>w;
			w=w+i;
			if(w>=max)
			{	
				max=w;
				
			}
			
		}
        
        ans=max;
		cout<<ans<<endl;
	}
	
	return 0;
}

