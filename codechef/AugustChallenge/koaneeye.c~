#include<stdio.h>
#include<stdlib.h>

int main()
{
  int i;
  char word[1000];
  i=0;
  
  while(1)
    {
      word[i++]=getchar();
      if(word[i-1]=='\n')break;

    }
  for(j=i-1;j>=0;j--)printf("%c",word[j]);


  return 0;
}
