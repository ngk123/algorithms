#include<stdio.h>
#include<stdlib.h>

#define max 10000000000000000
typedef long long int lli;
 lli  arr[200];
int main()
{
 
  
  arr[0]=1;
  for(i=1; ;i++)
    {
      arr[i]=arr[i-1]<<3;
      if(arr[i]>max)break;
    } 
  idx=i;


  return 0;
}
