#include<stdio.h>
#include<stdlib.h>

int main()
{
  
  int n,i;
  char s[100000],str[100000];


  scanf("%d",&n);
  i=0;
  a= getchar();
  s[0]=a;
  i++;
  while(1)
    {
      a=getchar();
      if(a=='\n'){i++;}
      s[j]=a;
      j++;
      if(i==n)break;
       
    }
  
  scanf("%s",str);
  flag=0;
  i=0;j=0;
  while(1)
    {

      if(str[i]=='<'){flag=1;i++;continue;}
      if(flag==1 && str[i]=='3' && s[j]=='\n'){i++;j++;flag=0;continue;}

      if(str[i]==s[j]){i++;j++;continue;}
      if(str[i]!=s[j]){i++;continue;}
      if()

    }

  return 0;
}
