#include<bits/stdc++.h>
using namespce std;

int main()
{	
	int k,len,i;
	vector<string> vs;
	vector<int> vc,vn;
	string str;
	cin >> str;
	cin >> k;
	len=str.length();
	for(i=1;i<len;i++)
	{
		if()
	}
	
	
	return 0;
}
